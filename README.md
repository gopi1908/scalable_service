# Ecommerce Angular Demo

Minimal Angular app with:
- Splash, Registration, Login
- Dashboard (searchable list), Detail page
- Dummy payment gateway and Orders
- Simple auth (localStorage token)

Run:

```bash
npm install
npx ng serve --open
```

You need the Angular CLI (or use `npx ng`).
